# Kasi Flavour Website

This repository contains the Kasi Flavour website built with HTML, CSS, and JavaScript.

## EmailJS Integration

The site now includes EmailJS support for form submission:

- `contact us.html` uses `contact-form` and sends via EmailJS template `template_fgu4ly9`
- `enquiry.html` uses `feedback-form` and sends via EmailJS template `template_jaaf44d`
- `scripts.js` includes EmailJS configuration:
  - Service ID: `service_n7q97i4`
  - Public Key: `3DSjkzL_H2FGsEfqk`

## Files Updated

- `scripts.js`
- `contact us.html`
- `enquiry.html`

## Next Steps

1. Confirm EmailJS templates match the form field names.
2. Test both forms in the browser to verify email delivery.
3. If needed, update the EmailJS template field names in `scripts.js`.
